// SUMMATION

function summation(n) {
    let sum = 0;

    for (let i = 1 ; i<=n ; i++) {
        sum += i
    }

    return sum;
}


// Factorial

function factorial(n) {
    let fact = 1;

    for (let i = 1 ; i <= n ; i++){
        fact = fact * i
    }

    return fact;
}
