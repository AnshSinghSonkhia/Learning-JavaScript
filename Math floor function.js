// This function will take 2.2598223 and return 2. The function will round a number down to the nearest integer.
// For example if we had the number, 2.9999, the function will round this input down to 2.

function getFloor(x) {
    value = Math.floor(x);
    return value;
}
